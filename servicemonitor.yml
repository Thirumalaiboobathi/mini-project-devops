apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: weatherservice-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: weatherservice
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
  namespaceSelector:
    matchNames:
      - default
