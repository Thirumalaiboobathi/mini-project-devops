apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: alertservice-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: alertservice
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
  namespaceSelector:
    matchNames:
      - default
